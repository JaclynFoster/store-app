//this is where they will set quantity of the animal with + or minus buttons and an Add button which will add to the cart
import React from 'react'

const AnimalForm = () => {
  return <div>Animal Form</div>
}

export default AnimalForm
